[build]
buildCommand = "cd backend && npm install --include=dev && npm run build"

[deploy]
startCommand = "cd backend && npm run db:migrate && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
